(ns grep\modules\input)

# already not used file.

(defn stream-line-seq [in-stream]
  (lazy-seq
    (let [line (php/fgets in-stream)]
      (when (not= false line)
        (cons line (stream-line-seq in-stream))))))

